apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: finance-tracker
  namespace: finance-tracker-prod
spec:
  type: kv-v2
  vaultAuthRef: vault-auth
  mount: secret
  path: kubernetes/finance-tracker/prod
  destination:
    name: finance-tracker-secret
    create: true
  refreshAfter: 1h
