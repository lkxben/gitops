apiVersion: v1
kind: Service
metadata:
  name: compute-node-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: compute-node-exporter
spec:
  ports:
    - name: metrics
      port: 9100
      targetPort: 9100
---
apiVersion: v1
kind: Endpoints
metadata:
  name: compute-node-exporter
  namespace: monitoring
subsets:
  - addresses:
      - ip: 192.168.20.50
    ports:
      - name: metrics
        port: 9100
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: compute-node-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: compute-node-exporter
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics