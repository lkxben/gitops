apiVersion: v1
kind: Service
metadata:
  name: pve-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: pve-exporter
spec:
  ports:
    - name: metrics
      port: 9221
      targetPort: 9221
---
apiVersion: v1
kind: Endpoints
metadata:
  name: pve-exporter
  namespace: monitoring
subsets:
  - addresses:
      - ip: 192.168.10.2
    ports:
      - name: metrics
        port: 9221
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pve-exporter
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: pve-exporter
  endpoints:
    - port: metrics
      interval: 30s
      path: /pve
      params:
        module:
          - default